interface:
  display_name: "Disable Grok Execution"
  short_description: "Disable Grok execution while preserving active work"
  default_prompt: "$grok-executor-off"
policy:
  allow_implicit_invocation: false
