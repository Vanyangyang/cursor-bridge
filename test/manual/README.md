# Manual smoke tests

These scripts exercise a live Cursor instance and are intentionally excluded from `npm test`.

Run them explicitly from the repository root with `node test/manual/<script>.mjs`. Generated reports are written to the ignored `.artifacts/` directory.
